import type { Theme, ViewType } from '../types'

export const THEME_LABEL: Record<Theme, string> = { light: '밝은 테마', dark: '어두운 테마' }

export const VIEW_TYPE_LABEL: Record<ViewType, string> = {
  list: '목록형 보기',
  gallery: '갤러리형 보기',
}
